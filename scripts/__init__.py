# scripts package — AllInOnePolyglotAIJDK tooling modules
#
# Modules:
#   synthesis_engine   — SynthesisAgent: deconstructs an arbitrary target file
#                        (binary, bytecode, or script) via static analysis + AI.
#   dual_synthesis     — DualSynthesizer: hard-coded Pattern Synthesis of two
#                        Slint core subsystems (text rendering + multi-window).
